#include <stdio.h>

int main() {
    int X;
    
    // Reading the input
    scanf("%d", &X);
    
    // Checking if the team qualifies
    if (X >= 12) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }
    
    return 0;
}
